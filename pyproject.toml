# pyproject.toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyflirt"
version = "0.1.0"
description = "Playful, seedable pickup lines for the terminal."
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "Team Quartz" }]
dependencies = []